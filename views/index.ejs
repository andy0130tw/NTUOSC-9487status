<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Who's there? - Project 9487Status</title>
  <link rel="stylesheet" href="/go/public/style.css">
</head>
<body>
  <div class="container">
    <h1>Who's @ NTUOSC?</h1>

    <p>Hello, the visitor from <%= clientIP || 'the other side' %>!</p>

    <ul>
    <% ipList.forEach((client) => { %>
      <li class="<% if (client.address == clientIP) { %>owner<% } %>"><!--
   --><span class="hostname"><%= client.hostname || '(no name)' %></span>: <%= client.last_seen %><% if (!client.wireless) { %> [cable]<% } %>
      </li>
    <% }); %>
    </ul>

    <footer>
      <p>
        Who's there? A project by Andy Pan, formerly by Viktor Lin.
      </p>
    </footer>
  </div>
</body>
</html>
